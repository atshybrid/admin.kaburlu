# Local service (default port 3096 — does not conflict with Next.js :3000)
PORT=3096
NODE_ENV=development

# PostgreSQL — local Docker recommended for dev (do not commit real passwords)
# Production: set DATABASE_URL in .env (never commit .env)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/kaburlu_block06

# Optional: skip DB on render (in-memory template only)
BLOCK06_SKIP_DB=false
